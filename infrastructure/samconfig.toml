# SAM CLI deploy configuration
# Usage: cd infrastructure && sam deploy
#
# Stage deploy:
#   sam deploy --config-env stage --parameter-overrides \
#     DailyApiKey=xxx DailyWebhookSecret=xxx

version = 0.1

[default.deploy.parameters]
stack_name = "audio-studio-dev"
region = "ap-south-1"
resolve_s3 = true
capabilities = "CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND"
confirm_changeset = true

parameter_overrides = [
  "Environment=dev",
  "DailyDomain=ak-kgen",
  "FrontendDomain=localhost",
  "FrontendOrigin=http://localhost:5173",
]

# Secrets — pass these at deploy time:
#   sam deploy --parameter-overrides \
#     DailyApiKey=xxx \
#     DailyWebhookSecret=xxx

[default.build.parameters]
use_container = false

[stage.deploy.parameters]
stack_name = "audio-studio-stage"
region = "ap-south-1"
resolve_s3 = true
capabilities = "CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND"
confirm_changeset = true

parameter_overrides = [
  "Environment=stage",
  "DailyDomain=stage-kgen",
  "FrontendDomain=stage-studio.kgen.io",
  "FrontendOrigin=https://stage-studio.kgen.io",
]

# FrontendOrigin — set to your Cloudflare domain
# Secrets — pass at deploy time:
#   sam deploy --config-env stage --parameter-overrides \
#     DailyApiKey=xxx DailyWebhookSecret=xxx

[stage.build.parameters]
use_container = false

[prod.deploy.parameters]
stack_name = "audio-studio-prod"
region = "ap-south-1"
resolve_s3 = true
capabilities = "CAPABILITY_NAMED_IAM CAPABILITY_AUTO_EXPAND"
confirm_changeset = true

parameter_overrides = [
  "Environment=prod",
  "DailyDomain=ak-kgen",
  "FrontendDomain=studio.kgen.io",
  "FrontendOrigin=https://studio.kgen.io",
]

[prod.build.parameters]
use_container = false
